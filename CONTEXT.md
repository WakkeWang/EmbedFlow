# EmbedFlow

中小嵌入式团队的内部构建/测试/发布系统，开源（MIT，单仓库 server/ + serial-client/）。部署在单台编译服务器上，串口设备经 Windows 串口客户端透传接入。界面中英文双语，预留其他语言。

架构主轴：领域内核（数据模型 + 状态机，纯 Go 无 IO）+ 执行器层（构建/串口会话/expect/SSH/报告），四类规则统一为一种配置对象。目标双轨：内部先用起来 + star 优化同步长（每个公开版本打磨可展示面：双语 README、截图、串口客户端体验）。

## Language

### 通用

**工程（Project）**:
一次产品交付的开发单元，如 YY0133。是所有配置的顶层容器。所有配置（构建项目、部署规则、设备、测试单元、测试项、测试集合、发布规则）默认工程级隔离；四类规则统一在配置中心页面管理（tab 分组），统一复制粘贴 + JSON 导出/导入。
_Avoid_: 项目、产品（「项目」保留给构建项目）

**构建项目（Build Item）**:
工程下一个可独立构建的单元，有源、命令、产物声明、版本提取命令，如 bootfw。
_Avoid_: 构建任务、构建配置

**前置项目（Prerequisite）**:
构建项目声明的前置构建项目（可多个，可跨工程引用）。前置分组：组内 OR（任一成功即可）、组间 AND（全部组成功才构建本项目）。前置失败则本项目跳过（标「跳过-前置失败」）；批次内同项目只构建一次；跨批次不自动复用。界面上须清晰展示与/或关系。
_Avoid_: 依赖（单独说）、依赖树

**设备（Device）**:
工程下登记的物理目标机，含 SSH 或串口信息。
_Avoid_: 目标机、靶机

**串口客户端（Serial Client）**:
测试人员 Windows 电脑上的 GUI 程序：扫描/打开/共享串口到服务器。纯透传，无业务逻辑，主动长连接服务器，断线重连。共享时指定所属设备。
_Avoid_: Agent、代理程序

**会话（Session）**:
服务器与一台设备之间的一次串口/SSH 交互过程，全程带时间戳日志。分任务会话（刷机/测试运行）与手动会话。一台设备同一时间只有一个活跃会话，按设备维度存历史，报告引用会话 ID。
_Avoid_: 连接、通道

**会话日志（Session Log）**:
一次会话的全量收发记录，带时间戳，类似 SecureCRT 会话记录。
_Avoid_: 串口日志、终端日志

### 构建板块

**批次（Batch）**:
一次「一键构建」产生的一条记录，本批选中的构建项目按前置关系构建。全局最多 N 个批次并行（系统设置，默认 4），超出的批次排队等待。
_Avoid_: 流水线、pipeline

**版本（Version）**:
构建项目配置一条版本提取命令（如 `ver.sh -a`），构建后执行，完整输出全部记录（系统版本、内核版本、补丁版本等），作为该次构建的版本信息。
_Avoid_: 版本号（不精确）、commit（那只是快照标识）

**构建记录（Build Record）**:
批次内一个构建项目的一次执行记录：源快照（commit）、日志、产物清单、版本提取输出。
_Avoid_: 构建历史、build

**产物（Artifact）**:
构建记录声明的输出文件，按构建记录归档，带校验和。路径必须相对构建目录。
_Avoid_: 输出文件、输出

**dirty**:
源 git 仓库有未提交改动。dirty 的源拒绝构建。
_Avoid_: 未提交、脏

### 部署板块

**部署规则（Deploy Rule）**:
工程级配置对象，描述「怎么部署」：方式（手动/SSH/刷机）+ 对应配置。部署 = 选构建记录 + 部署规则执行。
_Avoid_: 部署配置、部署方案

**部署记录（Deploy Record）**:
一次部署执行的结果记录（谁、何时、对哪台设备、用哪次构建、结果如何）。
_Avoid_: 部署历史

**手动部署（Manual Deploy）**:
下载产物 + 界面展示的部署步骤说明，人工执行。
_Avoid_: 自行部署

**SSH 部署**:
通过 SSH 向设备下发命令模板（可注入产物变量）执行的部署方式。
_Avoid_: 远程部署

**刷机（Flash）**:
经串口 expect 步骤序列执行的设备整机安装：reboot → uboot（等待串/密码/命令）→ ramdisk（登录/命令）→ 安装脚本 → 验证。
_Avoid_: 远程刷机、烧写

**expect 步骤（Expect Step）**:
刷机规则的最小执行单元：等待字符串 + 可选发送内容 + 超时 + 失败动作。刷机规则 = expect 步骤序列。
_Avoid_: 等待步骤、串口指令

### 测试板块

**测试用例单元（Test Case Unit）**:
最小可配置测试单元：串口/SSH 执行的命令（或脚本）+ 期望字符串 + 匹配方式 + PASS/FAIL 标准 + FAIL 后继续/等待。
_Avoid_: 用例、测试单元

**测试项（Test Item）**:
由多个 step 组成的具体测试，step = 一个测试用例单元或一个人为节点（观察/干预）。例：电源状态测试项。
_Avoid_: 测试步骤组、测试点

**人为观察（Human Observe）**:
step 的类型之一：测试人员看/听设备（LED、蜂鸣器）后在 Web 界面确认 PASS/FAIL。
_Avoid_: 人工观察、目测

**人为干预（Human Intervene）**:
step 的类型之一：测试人员对设备做物理操作（插拔电源线、上电下电）后在 Web 界面确认完成。
_Avoid_: 人工干预

**测试集合（Test Suite）**:
按系统组合测试项的集合，一个工程可有多套，对应不同安装系统。
_Avoid_: 测试集、测试计划

**测试运行（Test Run）**:
一次测试执行实例 = 设备 + 测试集合 + 关联构建/部署/发布记录。报告自动带版本。
_Avoid_: 测试任务、测试会话（会话指串口交互本身）

**版本查看（Version Check）**:
测试集合的第一个测试项：从设备读出版本与期望版本比对，期望版本默认取最新构建记录。
_Avoid_: 版本检查

### 发布板块

**发布规则（Release Rule）**:
工程级配置对象：目标目录（可多个，本机或远程 SSH）→ 备份目录（可选日期后缀）→ 同名先删后覆盖。发布 = 选发布规则 + 勾选构建记录，发布所勾选构建记录的全部产物。目标目录受管理员白名单约束。
_Avoid_: 发布配置

**发布记录（Release Record）**:
一次发布执行的结果记录，弹窗确认「备份/覆盖」后产生。
_Avoid_: 发布历史

### 系统级

**配置中心（Config Hub）**:
工程内统一管理四类规则（构建项目、部署规则、测试单元/项/集合、发布规则）+ 设备的页面，tab 分组，统一复制粘贴与 JSON 导出/导入。
_Avoid_: 设置页（那是系统级管理员设置）

**系统设置（System Settings）**:
管理员维护的全局项：最大并行批次数（默认 4）、产物校验方式（默认 sha256）、发布/备份根目录白名单、构建临时目录路径。
_Avoid_: 配置中心（那是工程级）
