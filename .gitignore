# Claude Code 本地配置
.claude/

.gstack/

# 内部真实值对照（密码/IP/路径），只留本地，绝不入库
docs/local/

# gitleaks 自定义规则：规则字面量含内部产品线名/路径（公司私有），不入库。
# CI 的 gitleaks job 在文件缺失时自动只跑默认规则（凭证类）。
.gitleaks.toml

# 运行时数据目录（服务器默认 -data ./data）
data/

# 本地构建产物（服务器二进制，含交叉编译拷贝件）
/embedflow-server*
